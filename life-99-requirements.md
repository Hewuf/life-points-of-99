# 人生 · 99（Life-99）— 需求与技术方案文档

> **本文档用途**：作为 Claude Code 的实现依据。当前项目前端已开发了一部分，本文档要求 **在现有项目基础上重构**，而非从零新建。请先阅读第 0 节与第 9 节的执行约定，再开始动手。

---

## 0. 给 Claude Code 的前置说明（先读这里）

1. **这是一次重构，不是 greenfield**。先盘点现有项目（技术栈、目录结构、已实现的组件与逻辑），保留可复用的部分，向本文档定义的目标形态迁移。不要在没有理解现有代码的情况下整体重写。具体步骤见第 9 节。
2. **开源 + 隐私是硬约束**。本项目会开源。任何个人内容（出生年、姓名、邮箱、人生记录文本、照片）**绝不能进代码仓库**，必须放在数据库/对象存储/环境变量里。详见第 9.4 节，请在编码全程遵守。
3. **范围克制**。V1 只服务于"作者一人编辑、所有人只读"。不要实现多用户注册体系（见第 2 节路线图）。遇到"为以后扩展先建一套"的冲动时，按文档里标注的阶段来做。
4. **文档里凡标注「默认值，可调」的数值**（动效时长、压缩系数、半径等）都是建议初值，集中在附录 B，便于统一调整。

---

## 1. 项目背景与设计理念

用"里程碑"的方式记录人的一生。把一生（99 岁）抽象成页面上的 **99 个光点**，以一条**蜿蜒向上流动的河**串起，让人直观看到自己人生的进度，从而珍惜时间、思考如何更有意义地度过这一生。

设计内核不是"记录工具"，而是用一个视觉隐喻让人直面"时间有限"。情感基调取**希望 / 梦幻**方向；当下那一年用**萤火虫呼吸感**的动效表达"正在发生、仍然鲜活"。

核心隐喻：

- **河自下而上流动** —— 底部是出生，越往上越接近未来；人只能向前/向上走。
- **走过的河面发光，前方的河面暗淡** —— 脚下的路是暖色、明亮的；未来只剩一条淡淡的、向顶部收细的痕。
- **未来被压缩在远处** —— 未来的点更小、更暗、间距更密，向页面顶部收束，像消失在地平线外。

---

## 2. 范围与路线图

### 2.1 V1 必做（本期核心）

- 主页：河流时间线（99 点、三态、萤火虫呼吸、自动定位、悬停摘要、点击进详情）。
- 详情页：查看 + 编辑（仅作者）。
- 权限：所有人只读、作者一人可编辑（Supabase Auth + RLS）。
- 主题：明 / 暗双主题。
- **情绪色（扩展 #3）并入 V1 核心**：它直接决定主页河上每个点的颜色，属于结构性需求，必须在 V1 落地。

### 2.2 本期纳入、可排在核心之后

- **标签筛选（扩展 #6）**：给关键词/事件打标签（如 事业/关系/健康/旅行），可筛选查看某条线索贯穿一生。
- **导出海报 / 人生之书（扩展 #4）**：把主页河流导出为可保存/可打印的图片或长图。**为此主页河流必须用 SVG 实现**，方便导出。

### 2.3 后续阶段（先不做，但数据/架构上不要堵死）

- **年末提醒邮件（扩展 #5）**：到年底提醒作者填写当年总结。注意：这是唯一需要后端定时任务的功能（Supabase Cron / Edge Function 或外部 cron），明确放到后续阶段。
- **多用户**：他人注册、登录、编辑自己的人生。这是另一个量级的工程（数据隔离、隐私、防滥用）。
- **分享 / 浏览他人的人生**（社交）。

### 2.4 明确不做

- 周粒度视图、未来点写期许、时间胶囊/纪念日/统计洞察 —— 本期均不做。

---

## 3. 核心概念与术语

- **lifespan**：一生的点数，固定 `99`。
- **birthYear**：作者出生年，**存数据库的 profile，不写死在代码里**。
- **currentYear**：当前日历年，前端从系统时间取。
- **某年的索引 i**：`i = 0 .. 98`。`year = birthYear + i`，`age = i`。
- **currentIndex**：`currentYear - birthYear`，即"今年"对应的点。
- **三态**：
  - **past（过去）**：`i < currentIndex`。正常大小、正常间距、有情绪色。
  - **present（当下）**：`i === currentIndex`。河流线上的同一个节点，叠加萤火虫呼吸。
  - **future（未来）**：`i > currentIndex`。更小、更暗、间距更密，向顶部收束。

---

## 4. 数据模型（Supabase / Postgres）

### 4.1 表：`life_profile`（V1 单行，作者本人）

| 字段                    | 类型        | 说明                                         |
| ----------------------- | ----------- | -------------------------------------------- |
| id                      | uuid pk     | 默认 `gen_random_uuid()`                     |
| owner_id                | uuid        | 关联 `auth.users.id`（作者）。为多用户预留。 |
| display_name            | text        | 可选，展示用名字                             |
| birth_year              | int         | **出生年，配置项，禁止写死在源码**           |
| lifespan                | int         | 默认 `99`                                    |
| created_at / updated_at | timestamptz |                                              |

### 4.2 表：`year_entries`（每年一条）

| 字段                    | 类型        | 约束 / 说明                                                  |
| ----------------------- | ----------- | ------------------------------------------------------------ |
| id                      | uuid pk     |                                                              |
| owner_id                | uuid        | 关联作者；RLS 写权限依据                                     |
| year                    | int         | not null，`(owner_id, year)` 唯一                            |
| title                   | text        | 一句话总结，≤ 50 字（前端校验）                              |
| description             | text        | 详细描述，不限字数                                           |
| keywords                | text[]      | 关键词/事件，每条一句话，**最多 8 条**                       |
| mood_score              | int         | 可空，`0–100`，映射到情绪色（见 7.2）。空值用中性色。        |
| tags                    | text[]      | 可空，为「标签筛选」预留（本期可后置实现，字段先建好）       |
| images                  | jsonb       | 有序数组，**最多 12 张**，元素形如 `{ "path": "...", "alt": "", "w": 1600, "h": 1067 }`，`path` 指向存储桶对象 |
| created_at / updated_at | timestamptz |                                                              |

> 设计取舍：图片用 `jsonb` 有序数组内联存储（单年 ≤12 张，量极小），无需单独建表；`path` 存对象 key，前端用它拼公开 URL。

### 4.3 对象存储

- Supabase Storage 建 bucket：`life-images`，**公开读**。
- 写入（上传/删除）仅作者可执行（通过 Storage policy + 登录态约束）。
- V1 用 Supabase Storage 即可；若日后访问量大、出口流量成为成本，可迁移到 Cloudflare R2（数据库只存 key/URL，迁移成本低）。

### 4.4 RLS（行级安全）—— 权限模型的核心

对 `life_profile` 与 `year_entries` 启用 RLS：

- **读**：允许所有人（`anon` 角色），即默认公开只读。
- **增/改/删**：仅 `auth.uid() = owner_id`。

策略意图示例（具体语法以 Supabase 当前版本为准）：

```sql
-- 任何人可读
create policy "public read" on year_entries
  for select using (true);

-- 仅作者可写
create policy "owner write" on year_entries
  for all using (auth.uid() = owner_id)
  with check (auth.uid() = owner_id);
```

> 因此"别人只读"**不是靠前端拦截**实现的，而是数据库层默认放开读、写入被 RLS 挡住。前端是否显示编辑入口只是体验问题，安全由 RLS 保证。
>
> 说明：RLS 策略作用于 Postgres 角色（`anon` / `authenticated`），这与 2025 年新版 API key 命名无关——publishable key 的未登录请求仍以 `anon` 角色访问数据库，所以上述策略写法不变。

---

## 5. 功能需求

### 5.1 主页：河流时间线 ★核心

**布局与方向**

- 河 **自下而上**：底部 = 出生（`i=0`），顶部 = 最远的未来（`i=98`）。
- **竖向滚动**：内容高度超出一屏，靠竖向滚动浏览。河整体竖向流动、**弱化弯曲**（仅小幅左右摆动，是"一条向上流的河"而非来回折返的蛇形）。
- **打开页面自动滚动定位到呼吸点（present）**，并让它落在视口**偏下**的位置：下方能看到发光的过去，上方能看到收束的未来——一开页面就站在"现在"。
- 响应式：移动端宽度收窄，左右摆动幅度随之缩小。

**99 个点的分布与三态**

- 点沿河（SVG path）按**加权弧长**分布：
  - past / present 段：等距（正常间距）。
  - future 段：间距随距离递减、越远越密（向顶部收束）。
- **past 点**：正常半径、情绪色（见 7.2）、不透明度高。
- **present 点**：就是河流线上"今年"那个节点本身，叠加萤火虫呼吸（见 7.3）。呼吸光必须从该节点发出，**不得是河外的独立元素**。
- **future 点**：半径随距离衰减、不透明度衰减、用中性冷色；向顶部越来越小越暗。
- **河面分两段着色**：
  - 已度过段（底部 → present）：暖色、明亮、可带极轻微"流动"微光。
  - 未来段（present → 顶部）：淡、细，向顶部进一步收细/淡出。
  - present 点位于两段交界。

**交互**

- 悬停任意 **past / present** 点：浮出小卡片，展示 `年份`、`X 岁`、`状态`、`一句话总结`（无内容时显示占位文案）。future 点不响应悬停。
- 点击 **past / present** 点：跳转该年详情页（`/year/:year`）。**future 点不可点击**（光标默认态、视觉淡）。
- 顶部展示**进度读数**作为「情绪基调文案」的占位：用中性百分比/计数，如 `已度过 33%` 或 `32 / 99`。最终情绪化文案待定，先用中性占位，不要写带强烈情绪导向的句子。
- 主题切换入口（明/暗）。

**实现优先级（建议顺序）**：① 河 + 99 点三态静态形态 → ② 自下而上 + 竖向滚动 + 自动定位 present → ③ 萤火虫呼吸 + reduced-motion 降级 → ④ 已度过/未来两段着色 → ⑤ 情绪色 → ⑥ 悬停卡片 + 点击跳转。

> 河流生成、弧长分布、三态、萤火虫的具体算法见**附录 A**；可参考随附的 HTML 原型（附录 C）理解视觉目标，但 React 实现请按附录 A 重写，不要直接套 HTML。

### 5.2 详情页（`/year/:year`）

**查看模式（所有人）**

- 标题：当年一句话总结（对应主页 ≤50 字总结）。
- 详细描述：不限字数。
- 关键词 / 事件：每条一句话，最多 8 条。
- 图片：最多 12 张，支持点击放大查看。

**编辑模式（仅作者，登录后可见入口）**

- 「编辑」按钮进入编辑态，可修改上述所有字段。
- 图片：上传到 `life-images` 桶（上传为需要确认的操作；删除图片属于**永久删除**，需二次确认）。可调整顺序。
- 可设置 `mood_score`（情绪色）与 `tags`。
- 保存写回 `year_entries`。
- 未登录或非作者：不显示编辑入口；即便构造请求，RLS 也会拒绝写入。

### 5.3 登录与权限

- **公开只读无需登录**：访客直接用 Supabase **publishable key**（`sb_publishable_…`，2025 年新密钥模型，替代旧的 `anon` key）读取数据。
- **作者登录**：`/login` 路由，采用 **Magic Link（邮箱免密登录）**。作者邮箱放环境变量，**不写死在源码**。
- 登录后前端持有作者身份（`auth.uid()`），写操作被 RLS 放行。
- 全站只有一个账号（作者本人）。**不实现注册流程。**
- 前端只使用 **publishable key**（`sb_publishable_…`，可公开，安全由 RLS 兜底）直连 Supabase；**绝不在前端或仓库出现 secret key（`sb_secret_…`，替代旧的 `service_role` key）**。

### 5.4 主题（明 / 暗）

- 通过 `data-theme="dark|light"` + CSS 变量切换（见 7.1 色彩 token）。
- 默认主题：跟随系统 `prefers-color-scheme`；用户手动切换后用 `localStorage` 持久化（真实 Web 应用允许使用 localStorage）。
- 暗色为"夜晚萤火虫"基调，亮色为"晨曦"基调。

### 5.5 本期扩展功能

- **情绪色（#3，核心）**：见 4.2 `mood_score` 与 7.2 标度。主页 past 点按 mood 着色，整条河成为一条横跨一生的情绪光谱。
- **标签筛选（#6）**：`year_entries.tags` 字段本期先建好；筛选 UI 可作为核心完成后的增量。筛选时高亮/淡化主页对应年份的点。
- **导出海报（#4）**：把主页河流 SVG 导出为图片/可打印长图。因此**主页必须用 SVG 渲染河与点**。导出动作属于下载，需用户确认。

---

## 6. 技术栈与架构

**前端**

- **React**（保留现有项目所用版本/构建工具；若现有项目无明确约定，建议 Vite + React + TypeScript）。
- **TypeScript** 强烈建议（利于重构与协作）。
- **路由**：React Router（路由表见第 10 节）。
- **样式**：CSS 变量 + `data-theme` 做主题；主页河/点/动效用 **SVG + CSS 动画**手写（不要用重组件库去套）。其余 UI 可用轻量方案（CSS Modules 或现有项目已有的方案），避免引入与定制视觉冲突的重型 UI 框架。
- **数据访问**：`@supabase/supabase-js`，前端直连，使用 **publishable key**（`sb_publishable_…`，放环境变量）。

**后端 / 数据**

- **Supabase**：Postgres（数据）+ Storage（图片）+ Auth（Magic Link）+ RLS（权限）。V1 **无需自建后端服务**。
- 出生年、lifespan 等配置从 `life_profile` 读取，而非硬编码。

**部署**

- 纯静态前端，部署到 Vercel / Netlify / Cloudflare Pages 之一。
- 环境变量在部署平台配置（见 9.4）。

> 为何不自建 Python 后端：本项目写入极低频、读多写少、结构化数据极小，Supabase + RLS 已能表达全部 V1 需求，自建后端属于过度设计。若未来确有多用户/复杂逻辑，再引入独立后端。

---

## 7. 视觉与设计规范

### 7.1 色彩 token（CSS 变量，明/暗双套）

以下为建议初值（可调），请定义为 CSS 变量并在两套主题下分别赋值：

**暗色（dark，默认夜晚）**

- 背景：深夜蓝紫渐变（顶部带一丝暖光，呼应"生命之初"），约 `#080b16 → #11172e`。
- 文本主色 `#ece9e1`，次级 `#8b91a8`。
- 已度过河面（暖）：`#f0b35a → #e5826a` 渐变。
- 未来河面（淡冷）：`rgba(150,170,215,.14)`。
- present 萤火虫：核心 `#ffd479`，光晕 `#ffbe55`。
- future 点：`#5d6c93`（低不透明度）。

**亮色（light，晨曦）**

- 背景：暖白 → 淡蓝晨曦渐变，约 `#fcf6ee → #eceefb`。
- 文本主色 `#2c2a34`，次级 `#797787`。
- 已度过河面（暖）：`#df8e2c → #cd6549`。
- 未来河面（淡冷）：`rgba(92,112,162,.18)`。
- present 萤火虫：核心 `#ef9f2b`，光晕 `#f3ad42`。
- future 点：`#a9b1cb`。

### 7.2 情绪色标度（mood_score → 颜色）

- `mood_score` 为 `0–100` 的连续值，映射到一条**冷 → 中性 → 暖**的渐变，使过去的点连成一条情绪光谱。
- 建议沿一组柔和色停插值（示例停点，由低到高）：`#6d9dc5`（冷/低落）→ `#82b29a` → `#c2b27f`（中性）→ `#e8b04b` → `#e07a5f`（暖/高昂）。
- `mood_score` 为空：用中性默认色（取标度中点的柔和色），不要让未填写的年份显得突兀。
- 保持整体低饱和、克制，避免花哨。

### 7.3 字体

- 中文：一款有文学/希望气质的衬线体（如 Noto Serif SC）。
- 数字 / 拉丁：一款有个性的衬线体（如 Fraunces）用于年份、年龄、进度等数字展示。
- **不要使用** Inter / Roboto / Arial / 系统默认无衬线等通用字体作为主视觉字体。

### 7.4 动效

- **萤火虫呼吸（仅 present 一个点）**：柔和光晕在约 `4.2s` 周期内做缓慢的明暗 + 轻微缩放循环（呼吸感来自"慢"）；可叠加极小幅度（±2px 量级）的浮动漂移模拟萤火虫浮游。**只让当下这一个点动**，多个点同时闪会变成圣诞树。
- **河流流动**：已度过河面可叠加极轻微、缓慢的流光，幅度要克制。
- **页面载入**：可做一次轻微的渐入/铺陈，避免零散的微交互。
- **`prefers-reduced-motion: reduce` 必须降级**：关闭呼吸/漂移/流光，present 以静态柔光呈现。可访问性与专业度都依赖这一点。

---

## 8. 非功能需求

- **可访问性**：尊重 `prefers-reduced-motion`；点支持键盘可达与焦点态；图片填写 `alt`；悬停信息也应有非悬停的获取方式（移动端无 hover，点击/聚焦即可看摘要）。
- **响应式**：桌面与移动端均良好；移动端河身变窄、摆动收敛、自动定位 present 仍生效。
- **性能**：详情页图片懒加载；主页主要是 SVG，避免一次性加载所有原图（主页不展示原图）。
- **隐私/开源**：见第 9.4 节，作为验收项之一。
- **SEO**：基础 meta 即可，非重点。

---

## 9. 重构指引（给 Claude Code 的执行步骤）

### 9.1 第一步：盘点现有项目

- 输出现状：使用的框架/构建工具/语言、目录结构、已实现的页面与组件、已接入的数据/鉴权（如有）。
- 标注哪些可复用、哪些需要替换、哪些缺失。
- **在改动前先与既有约定对齐**：若现有项目已用某构建工具/语言/样式方案，优先沿用，除非与本文档目标冲突。

### 9.2 第二步：对齐目标结构

建议目录结构（**作为建议，按现有项目实际调整，不要为结构而结构**）：

```
src/
  pages/
    HomePage.tsx          # 河流时间线
    YearDetailPage.tsx    # 详情（查看/编辑）
    LoginPage.tsx         # Magic Link 登录
  components/
    river/                # 河与点的 SVG 渲染、三态、萤火虫
    DetailView.tsx
    DetailEditor.tsx
    ThemeToggle.tsx
    YearTooltip.tsx
  lib/
    supabase.ts           # 客户端（读 env）
    river-geometry.ts     # 蜿蜒生成 + 弧长分布（见附录 A）
    mood.ts               # mood_score → color
    theme.ts
  hooks/
    useProfile.ts         # 读 life_profile（birthYear/lifespan）
    useYearEntries.ts
    useAuth.ts
  styles/
    tokens.css            # 明/暗 CSS 变量
```

### 9.3 第三步：迁移顺序

1. 接好 Supabase 客户端与 env、建表与 RLS、建存储桶。
   - **Supabase 项目本身（注册账号、创建项目、获取 URL/密钥、点击 magic link 登录、配置 Auth 回调域名）由作者本人完成**，这些涉及账号与密钥，不由 Claude Code 代办。
   - 建表、RLS 策略、存储桶、TypeScript 类型生成、客户端接线等代码/SQL 工作由 Claude Code 完成。**若作者已为 Claude Code 接入官方 Supabase MCP server，则上述建表/策略/拉取配置可由 Claude Code 通过 MCP 直接执行**；否则 Claude Code 产出 migration SQL，由作者贴入 Dashboard SQL Editor 或用 Supabase CLI 执行。
2. `useProfile` / `useYearEntries` 数据层跑通（读）。
3. 主页：按附录 A 实现河 + 99 点三态静态形态。
4. 自下而上方向 + 竖向滚动 + 自动定位 present。
5. 萤火虫呼吸 + reduced-motion 降级。
6. 两段着色 + 情绪色。
7. 悬停卡片 + 点击跳转详情。
8. 详情页查看态。
9. Magic Link 登录 + 详情页编辑态 + 图片上传（写路径走 RLS）。
10. 明/暗主题完善与持久化。
11. 标签字段/筛选、海报导出（本期增量）。

### 9.4 开源与隐私清单（编码全程遵守，作为验收项）

- 所有密钥、邮箱、连接串放 **环境变量**；仓库提交 `.env.example`（占位假值），真实 `.env` 加入 `.gitignore`。
- **出生年、姓名、邮箱、人生记录文本、照片一律不进仓库**：出生年/姓名进 `life_profile`，作者邮箱进 env，内容与图片进数据库/存储桶。
- 仓库里**不出现** secret key（`sb_secret_…`，替代旧 `service_role`）；前端只用 publishable key（`sb_publishable_…`，可公开，安全由 RLS 兜底）。
- `public/` 等静态目录不要放真实个人照片。
- 注意 **git 历史**：从第一个 commit 起就不要提交任何敏感信息（事后删除仍会留在历史里）。
- 自检：克隆一份纯净仓库、不带任何 env 与数据，应当能跑起来一个"空人生"，且不泄露任何个人信息。

---

## 10. 路由

| 路径          | 页面               | 权限                 |
| ------------- | ------------------ | -------------------- |
| `/`           | 主页（河流时间线） | 公开只读             |
| `/year/:year` | 某年详情           | 查看公开；编辑仅作者 |
| `/login`      | Magic Link 登录    | 公开（仅作者会用）   |

- 详情页对 future 年份：可直接 404 或重定向回主页（future 不可进入）。

---

## 附录 A：河流主页核心算法

> 目标：在 React 中用 SVG 复现原型的视觉。以下为关键、非显然的实现要点。

1. **生成蜿蜒中线（自下而上、弱弯曲）**
   - 以**底部为起点、顶部为终点**生成一条竖向流动、左右小幅摆动的中线。
   - 用一组路径点（waypoints）+ Catmull-Rom 转贝塞尔得到平滑 path：对每段 `Pi→Pi+1`，控制点
     `cp1 = Pi + (Pi+1 − Pi−1)/6`，`cp2 = Pi+1 − (Pi+2 − Pi)/6`（端点处复制边界点）。
   - 摆动幅度相对画布宽度要小（"不那么婉转"）。整条 path 的**高度随点数撑开**，超出视口由竖向滚动查看。

2. **沿 path 按加权弧长分布 99 个点**
   - 取 `path.getTotalLength()`；对每个点 `i` 计算累计权重位置，再 `path.getPointAtLength(len_i)` 得坐标。
   - 权重：`i ≤ currentIndex` 时 `w=1.0`（等距）；`i > currentIndex` 时 `w` 随距离衰减（默认 `max(0.24, 0.42 * 0.965^k)`，`k = i − currentIndex`），实现未来越远越密、向顶部收束。
   - 归一化使总长落在 path 上（留一点末端余量）。

3. **三态渲染**
   - past：正常半径（默认 r≈6）、情绪色、高不透明度。
   - present：河上同一节点，叠加萤火虫（见下）。
   - future：半径与不透明度随 `k` 衰减（默认 `r=max(2.1, 5.4*0.972^k)`，`opacity=max(0.16, 0.62*0.955^k)`），中性冷色。

4. **两段着色**
   - 已度过河面：从底部到 present 的弧段，用暖色描边、明亮；可用 `stroke-dasharray = "livedLen totalLen"` 只画前段。
   - 未来河面：全程一条很淡的底色描边垫底；向顶部可再收细/淡出。

5. **萤火虫呼吸（仅 present）**
   - 光晕圆做 `scale + opacity` 的 ~4.2s `ease-in-out` 循环；核心点轻微同步脉动；外层组可加 ±2px 级别缓慢漂移。
   - SVG 元素动画用 `transform-box: fill-box; transform-origin: center;`。
   - `@media (prefers-reduced-motion: reduce)` 下全部关停，present 显示静态柔光。

6. **自动定位 present**
   - 首次渲染、布局完成后，把滚动位置设到 present 节点处，使其落在视口偏下（约 55–60% 高度处），让下方的"过去"与上方的"未来"都可见。

7. **悬停 / 点击**
   - past/present 点：悬停出摘要卡（年份/年龄/状态/一句话），点击进 `/year/:year`。
   - future 点：不响应。

---

## 附录 B：推荐默认参数（集中可调）

| 参数             | 默认值                      | 含义                       |
| ---------------- | --------------------------- | -------------------------- |
| lifespan         | 99                          | 点数                       |
| 未来间距权重     | `max(0.24, 0.42 * 0.965^k)` | 越大越疏；调小让未来更紧迫 |
| past 点半径      | 6                           |                            |
| present 核心半径 | 7                           |                            |
| future 半径      | `max(2.1, 5.4 * 0.972^k)`   |                            |
| future 不透明度  | `max(0.16, 0.62 * 0.955^k)` |                            |
| 呼吸周期         | 4.2s                        | 越慢越"呼吸"               |
| 漂移幅度         | ±2px 级                     | 萤火虫浮游感               |
| 自动定位高度     | 视口 55–60%                 | present 落点               |
| title 上限       | 50 字                       |                            |
| 关键词上限       | 8 条                        |                            |
| 图片上限         | 12 张                       |                            |

---

## 附录 C：原型参考

随本文档提供了一个 HTML 可交互原型（`life-99-river-prototype.html`），用于传达河流、三态、萤火虫呼吸的**视觉目标**。注意：

- 原型为上一版**自上而下、较强蛇形**的版本；本文档的正式要求是**自下而上、弱弯曲、竖向滚动、自动定位 present**，以本文档为准。
- 原型是单文件 HTML，React 实现请按附录 A 重写组织，不要直接搬运。
- 原型中"已度过河面发光/未来淡"、情绪色占位、明暗主题、reduced-motion 降级等，均为本文档要保留的方向。

---

*文档结束。如与现有项目存在冲突或本文档未覆盖的细节，请在动手前提出，对齐后再实现。*